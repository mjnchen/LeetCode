/**
 * Regular Expression Matching
 * Created by jchen on 8/16/15.
 */
public class LeetCodeOJ10 {
    public boolean isMatch(String s, String p) {
        // int n = s.length();
        // int m = p.length();
        // boolean[][] matchMatrix = new boolean[n][m];

        // for(int j = 0; j < m; j++){
        //     for(int i = j; j < n; j++){
        //         if(j == 0 && p.charAt(j) == s.charAt(i)){
        //             matchMatrix[j];
        //         }
        //     }
        // }
        return true;
    }
}
